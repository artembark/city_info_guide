import 'package:flutter/material.dart';

class ScheduleResultPage extends StatelessWidget {
  const ScheduleResultPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
